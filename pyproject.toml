[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "ambitdocs"
authors = [{name = "Ambit Team", email = "info@ambitsentry.co.za"}]
dynamic = ["version", "description"]
